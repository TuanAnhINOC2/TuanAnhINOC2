# AGENTS.md

## Cursor Cloud specific instructions

This repository (`TuanAnhINOC2/TuanAnhINOC2`) is a GitHub **special profile README
repository** — the repo name matches the account username, so GitHub renders its
`README.md` on the owner's public profile page.

There is **no application, build system, dependency manifest, automated test suite, or
setup script** in this repo. It currently contains only `README.md`.

Implications for future agents:

- **Update script / dependency install:** none required. There is nothing to install
  (no `package.json`, `requirements.txt`, etc.). The startup update script is a no-op.
- **Build / lint / test / run:** there are no such commands. The only "build" is GitHub
  rendering `README.md` as Markdown.
- To preview changes, render `README.md` with any Markdown viewer (e.g. GitHub's preview,
  or a local tool) — there is no server to start.

If real application code is added later, replace this section with the actual
install/build/lint/test/run instructions.
